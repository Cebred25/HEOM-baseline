[project]
name = "fmo-nz-kernel"
version = "0.1.0"
description = "Non‑Markovian kernel toolkit for FMO models"
authors = [ { name = "" } ]
readme = "README.md"
license = { text = "MIT" }
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "toml",
]

<<<<<<< HEAD
=======
[project.optional-dependencies]
heom = ["qutip>=5.0"]

>>>>>>> b1437fe (Add FMO NZ kernel scripts and update packaging metadata)
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"